import styled from 'styled-components/native';

export const AppFieldLabelStyle = styled.Text`
  font-size: ${props => props.theme.fontSize.sm};
  text-align: left;
  width: 100%;
  margin-bottom: 8px;
  font-family: ${props => props.theme.fontFamily.poppins_semibold};
`;
